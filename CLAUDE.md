# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 言語

コード内のコメント・ドキュメント・ユーザー向け文字列（メニュー項目名、エラーダイアログ）はすべて日本語。既存の文体に合わせること。テスト関数名も日本語。

## ビルド・実行

```powershell
cargo build --release          # target/release/extrun.exe と extrun-make.exe
cargo build                    # デバッグビルド（コンソールが残る）
cargo test                     # パーサとメニュー構造のテスト
cargo test --bin extrun-make   # 設定づくりの組み立てだけ
cargo clippy --all-targets
cargo fmt
```

**デバッグビルドとリリースビルドの決定的な違い**: `build.rs` はリリース時のみ `/SUBSYSTEM:WINDOWS` を指定する。つまりリリースバイナリにはコンソールが無く、`eprintln!` / `println!` の出力はどこにも表示されない。ユーザーに何かを伝える手段は `MessageBoxW`（エラーダイアログ）と `console.rs`（`--check` / `--version` / `--help`）の 2 つだけ。

同じ理由で、**リリースバイナリを PowerShell から起動すると終了を待たない**（`$LASTEXITCODE` も設定されない）。`--check` の終了コードを見るときは `Start-Process -Wait -PassThru` の `ExitCode` を使う。コマンドプロンプトは待つので `%errorlevel%` がそのまま使える。

`build.rs` は VERSIONINFO も埋め込む。リソーススクリプトは Cargo.toml のバージョンから `OUT_DIR` に生成するので、`.rc` をリポジトリに置かない（バージョンの二重管理になる）。コンパイルには Windows SDK の `rc.exe` が要る。

Windows 専用。`menu.rs` / `launch.rs` / `confirm.rs` / `console.rs` は `windows-sys` を無条件に use しているため、他プラットフォームではコンパイルできない。

実行には引数としてファイル/フォルダのパスが必要:

```powershell
cargo run -- "C:\path\to\file.txt" "C:\path\to\another.png"
extrun.exe --check             # 設定ファイルの検証（引数不要。エラーがあれば終了コード 1）
extrun.exe --version           # バージョン
extrun.exe --help              # 使い方
```

## リリース

```powershell
.\packaging\build-release.ps1   # dist\extrun-<version>-win-x64.zip と .sha256
```

テスト → リリースビルド → 組み立て → zip → SHA256 を通しで行う。バージョンは `cargo metadata` 経由で `Cargo.toml` から読むので、スクリプトにハードコードしない。zip の中身は `extrun-<version>/` の下に `extrun.exe` / `readme.txt`（`packaging/readme.txt` 由来。GitHub の `README.md` とは別物）/ `extrun-config.sample.txt`（`extrun-config.txt` をリネーム）/ `extrun-config-format.md` と `extrun-recipes.md`（`docs/` 由来。**zip の中ではフラットに並べる** — 2〜3 個のために展開した人にフォルダを掘らせない。両方が同じ階層に来るので、レシピ集から仕様書への相対リンクは GitHub 上でも zip の中でも通る）/ `CHANGELOG.md` / `LICENSE`。

- **右クリックメニューへの直接登録は勧めない方針**（1.2.0 で決定）。`shell\...\command` に `"%1"` を書く形式は選択されたファイルの数だけプロセスが起動し、`+`（まとめて渡す）が原理的に効かない。さらに `MultiSelectModel` 未指定＝`Document` 扱いのため、**16 個以上選ぶと項目自体がメニューから消える**。1 回の起動で全パスを受け取るには COM の DropTarget / ExecuteCommand ハンドラが要り、常駐しない設計とも `windows-sys` のみという方針とも両立しない。「送る」の SendTo ハンドラはその COM 実装なので制限を受けない。この判断により `.reg` の同梱と `--register` / `--unregister` の実装は取りやめ、`.gitattributes` の `working-tree-encoding` と `build-release.ps1` の `Copy-AsRegFile` も消えた。**「ExtRun はレジストリを書かない」という約束を保てるのもこの判断の効果**なので、右クリック登録を復活させる話が出たらここを読み直すこと。
- **設定ファイルを `.sample.txt` にリネームして入れるのは意図的**。`extrun-config.txt` のまま同梱すると、更新版を同じフォルダに展開したユーザーの設定が上書きで消える。
- **`LICENSE` の同梱は MIT の条件**（all copies に著作権表示を含める）なので外さない。
- 同梱するファイルを増減するときは、ビルドスクリプトと `packaging/readme.txt` の「同梱ファイル」節の両方を直す。
- バージョンを上げるときに手で直すのは `Cargo.toml` / `packaging/readme.txt` の見出し / `CHANGELOG.md` の 3 か所。readme.txt の見出しがずれているとビルドスクリプトが止まる。`Cargo.lock` は `cargo build` が追随するので手作業は不要。上げたら `.\packaging\build-release.ps1` を通して、zip 名・`--version`・exe の VERSIONINFO まで揃っているか確かめる。
- **ドキュメントの例示にバージョン番号を書かない。** 版が上がるたびに腐り、更新箇所が増えていく。`extrun-<version>-win-x64.zip` のような書き方にするか、明らかに架空の `v1.2.3` を使う。
- **開発中はバージョンを上げない。** 変更のたびに `CHANGELOG.md` の `[Unreleased]` へ追記していき、リリースを決めた時点で上の 3 か所を 1 回だけ揃える。`Cargo.toml` の値は VERSIONINFO として exe に埋め込まれ、zip 名と readme.txt の見出し照合にも使われる「配布物の身元」なので、開発中に上げると同じ番号を名乗る未公開バイナリが増えて不具合報告と対応づかなくなる。minor か major かも変更が出揃うまで決まらない。
- `v1.2.3` 形式のタグを push すると `.github/workflows/release.yml` が同じスクリプトを走らせ、zip と `.sha256` を**下書きの** Release に添付する。公開は GitHub 上で手動。

## アーキテクチャ

起動 → メニュー表示 → 選択されたコマンドを spawn → 即終了、という一発実行のプロセス。常駐しない。自前のメッセージループは無く、`TrackPopupMenu` を `TPM_RETURNCMD` で同期的に呼んで戻り値からアクションを引く（`window_proc` はほぼ `DefWindowProcW` への委譲で、`select-first` のタイマーだけを受ける）。実行中にファイルを書き出すことは一切ない。

**中身は `src/lib.rs`（ライブラリ）にあり、`src/main.rs` は引数を読んで呼び分けるだけの入口。** 引数の解釈（`take_options` / `take_config_path` / `Overrides`）だけが実行ファイル固有なので `main.rs` に残し、`Target` と `show_error_dialog` は複数のモジュールから参照されるので `lib.rs` に置く。ライブラリに切り出してあるのは、設定ファイルの数行を組み立てる 2 つ目の実行ファイル（`extrun-make`）が、**エスケープの規則（`text.rs`）とコマンドラインの組み立て（`invoke::resolve_invocations`）を同じ実装のまま呼ぶ**ため。書き直すと「`SPECIALS` が 2 か所に分かれて片方だけ直す」事故をプロセスをまたいで再現することになり、ライブプレビューも「表示と実行がずれない」という不変条件の外側に出る。

- **`pub mod` にするのは外から実際に呼ばれているものだけ**（現在は `check` / `config` / `console` / `menu` / `preview`）。残りは `pub(crate) mod` に留める。全部を `pub` にすると使われなくなった関数への `dead_code` 警告が出なくなり、消し忘れが静かに残る。必要になった時点で 1 つずつ開け、**なぜ外から要るのかを添える**。
- **ライブラリになったのでドキュメントの ```` ``` ```` が doctest として走る。** 言語名の無いフェンスは Rust として compile されるので、コードでない図には ```` ```text ```` と書く（`dialog.rs` の `DLGTEMPLATE` の図がこれで落ちた）。

データの流れ:

1. **`main.rs`** — 引数を絶対パスに正規化し `Target { file_type, path }` を作る。`file_type` は**フォルダなら `"folder"`、拡張子があれば小文字化した `".txt"`（先頭ドット付き）、拡張子が無ければ `"file"`**。この文字列がそのまま設定ファイルの拡張子指定と比較される。
2. **`config.rs`** — `extrun.exe` と同じディレクトリの `extrun-config.txt` を読んでパースする（カレントディレクトリではない）。書式は `docs/extrun-config-format.md` が正典。パースは診断メッセージ（`Diag`）を集めながら進み、エラーがあれば `main.rs` が行番号付きダイアログを出して終了する。
2b. **`text.rs`** — 設定ファイルの文字列を扱う部品。エスケープ（`^X`）の解決・区切り文字での分割・`%NAME%` の展開・引数の分解。**パーサ（`config.rs`）と実行時の置換（`placeholder.rs`）の両方から使う**ので、どちらか片方の都合で振る舞いを変えない。`Diag` を出す関数（`warn_accesskey` / `warn_stray_caret`）は診断がパーサの仕事なので `config.rs` に残してある。
3. **`menu.rs`** — Win32 のポップアップメニューを構築して表示し、選択結果を `execute_command` で実行に流す。**Win32 のメニューそのものに関わる処理だけを置く**（かつては下の 4 つも同居していて、メニューを表示しない `preview.rs` が `menu::` を参照するという歪みが出ていた）。
3b. **`filter.rs`** — 対象に合う項目だけを残す（`filter_menu_items`）。フィルタで穴が空いたセパレーターの整理もここ。
3c. **`invoke.rs`** — 起動 1 回ぶんの内容を組み立てる（`resolve_invocations`）。**実行と `--preview` の両方がここを通る。**
3d. **`launch.rs`** — 組み立てたプロセスを起動する（`spawn_command` / `launch_all`）。`:delay` と `:wait` の順序制御、`:admin` の昇格、起動失敗の報告。
3e. **`confirm.rs`** — 入力欄（`$?{...}`）と実行前の確認。**順番は入力欄 → 確認 → 起動。**
4. **`placeholder.rs`** — `PathPlaceholders` が 1 パスぶんの置換値を事前計算して保持する（HashMap を使わないのは起動速度優先の意図的な設計）。`RunContext` は**対象のパスから導けない実行時の値**（現在は日時のみ）を持ち、対象をまたいで共有される。
5. **`datetime.rs`** — 日時プレースホルダー `$t{...}` の書式解釈と検証。`LocalTime` は `GetLocalTime` を包むだけで、書式の対応表は `LocalTime::field` の 1 か所。
6. **`prompt.rs`** — `$?{...}` の入力ダイアログと書式の解釈。テンプレートの組み立ては `dialog.rs` に任せ、ここは入力欄という中身だけを持つ。
6b. **`dialog.rs`** — `DLGTEMPLATE` を組み立てる部品。Win32 に既製の入力ボックスも進行状況ダイアログも無いので、メモリ上に組み立てて `DialogBoxIndirectParamW` に渡す。`prompt.rs` と `progress.rs` が使う。
6c. **`progress.rs`** — `:delay` / `:wait` の進行状況ダイアログと、中止したあとの要約（残りのパスをクリップボードへ）。
7. **`icon.rs`** — `:icon` のアイコンをメニュー用のビットマップにする。`SHDefExtractIconW` で取り出し、32bpp の DIB に描き直す。
8. **`check.rs` / `preview.rs` / `console.rs`** — `--check` と `--preview` 専用。どちらもメニュー構築から独立している。`console.rs` は `--version` / `--help` の出力にも使う。

### 注意の要る不変条件

- **表示と実行がずれない**: 起動されるプロセスを組み立てるのは `invoke::resolve_invocations` の 1 か所だけで、実行経路（`execute_command`）と `--preview` の両方がここを通る。コマンドラインの組み立てを他所に書くと、プレビューが嘘をつくようになる。
- **`$i` は対象ごと・`$c` は共有**: 総数（`$c`）は対象をまたいで変わらないので `RunContext` に置き、何番目か（`$i`）は `PathPlaceholders::index` が持つ。**`index` の既定は 1** なので、最初の対象を基準に解決する `:dir` / `:confirm` / 入力欄はそのままで正しい（差し替えるのは `resolve_invocations` の対象ごとの経路だけ）。**ゼロ埋めの桁を総数から決めるのはこの分担のおかげ** — 桁を数えるのに `RunContext` 以外を見に行く必要がない。`$i{000}` の中に書けるのは `0` だけで、個数がそのまま桁になる（`$t{yyyy}` と同じで「見たままの形が出る」方が読み間違えない）。桁の指定でない `{...}` は素通しする（`$t` が `$t{` のときだけ日時なのと同じ）。
- **実行時に決まる値は `RunContext` に集める**: 日時は `execute_command` と `preview::run` の入口で 1 回だけ確定させ、全対象で共有する。対象ごとに取り直すと、複数選択の個別実行で `$t{ss}` がずれてファイル名が揃わなくなる。将来 `$?{...}`（入力プロンプト）のような「パスから導けない値」を足すときもここに載せる。
- **`$t` は `$t{` のときだけ日時**: 単独の `$t` を素通しするのは、既存の設定ファイルの意味を変えないため。書式の対応表は `datetime.rs` の `LocalTime::field` だけに置き、`validate_spec` もその関数を呼んで判定する（表が 2 か所に分かれると片方だけ直す事故になる）。中括弧の中に書式以外の英字を書けないのは、引用記法を増やさずに書き間違いを `--check` で全部拾うための割り切り。**書式の誤りは警告ではなくエラー**（黙って誤った文字列がファイル名に入るのを防ぐ）。
- **`$?{...}` は答えを先に集めてから置換する**: `replace()` は対象の数だけ呼ばれるので、その場で聞くと入力欄が何度も出る。`confirm::ask_prompts` が起動より前に全部聞いて `RunContext` に入れ、`replace()` は引くだけにする。順番は**入力欄 → `:confirm` → 起動**（確認のメッセージに入力した値を入れられる）。**ひとつでもキャンセルされたら実行しない**（半端な入力のまま起動すると意図しない引数でコマンドが走る）。`$?{...}` の終端を探すときは `prompt::find_close` で中括弧の深さを数える（中に `$t{...}` を書けるようにするため）。`placeholder.rs` の `replace()` も同じ関数を使う。
- **`RunContext` の入力欄の見出しは書かれた文字列全体**（`$?int{幅}`）: 中括弧の中身だけにすると `$?{幅}` と `$?int{幅}` が同じ答えを共有する。書き方を解釈するのは `prompt::parse_at` の 1 か所で、`placeholder.rs` と `text::split_args` はそれを呼ぶ。
- **`$?list{...}` は検証の代わりに選ばせる**: 取りうる値が決まっている場面では、入力を検証するより一覧から選ばせる方が確実で書く側も楽。`CBS_DROPDOWNLIST`（打ち込めない）にしてあるので `Rule::check` は常に `Ok` を返し、`ask` の聞き直しループも通らない。**区切りは `,`** — `|` は欄の区切りなので `^|` と書かせることになる。選択肢に `,` を書けないのはその引き換えで、必要になったら区切り文字を変えるのではなく別の記法を足すこと。**コンボボックスの高さは「開いたときの一覧まで含めた」大きさ**を書く（入力欄と同じ高さにすると押しても何も見えない）。
- **入力値の決まりは閉じた集合**（`int` / `num` / `name` / `list`）: 正規表現を入れないのは、依存クレートが増えて「実行時の依存は `windows-sys` のみ」に反するため。文字数指定を入れないのは、防げる事故が具体的に無いから。**`name` の 255 文字だけは固定の上限として組み込む**（NTFS の名前 1 要素の上限で、ユーザーが決める値ではない）。パス全体の `MAX_PATH` は入力値がどこに入るか分からないので見ない。**決まりを満たさない入力は打ち切らず聞き直す**（打ち切ると「キャンセルと同じで何も起きない」になり、検証がかえって邪魔になる）。対応表は `Rule::check` の 1 か所で、`--check` の既定値の検証も同じ関数を通る。
- **`split_args` は `$t{` / `$?{` の中では区切らない**: `$?{$n の新しい名前}` のように説明に空白が入るため。**素の `{` を数えるのは中括弧の中だけ**（深さ 0 で数えると PowerShell のスクリプトブロックが引数をまたいで繋がり、深さ 1 以上で数えないと `$?{... {0} ...}` が途中で切れる）。**終端の探し方は `prompt::find_close` と揃える** — かつて `split_args` だけが素の `{` を無視していて、引数が割れたうえ「`$?{` が `}` で閉じられていません」という見当違いのエラーが出ていた。
- **`$p` かどうかの判定は `text::has_path_placeholder` の 1 か所**: `--check` の警告（`check::warn_embedded_path_placeholder`）と `invoke::all_mode_args` の両方がここを通る。かつて後者だけが素の `contains("$p")` で判定していて、`^$path` のようにエスケープした `$` を「`$p` がある」と誤解し、**`+` の項目で対象のパスがどこにも渡らないまま起動していた**（`--check` 側は正しく読めていたので警告も出なかった）。エスケープを解する判定を 2 か所に分けると必ずこうなる。
- **ダイアログテンプレートは手で組む**: Win32 に入力欄つきダイアログの API が無いので、`dialog.rs` が `DLGTEMPLATE` をメモリ上に組み立てる。**各項目は DWORD 境界から始める**必要があり、先頭も DWORD 境界に置く（`Vec<u16>` では足りないので `Vec<u32>` に載せ替える）。組み立てを誤ると `DialogBoxIndirectParamW` が `-1` を返すだけなので、実機で通す `#[ignore]` 付きのテストを置いてある（`cargo test -- --ignored`。題名でウィンドウを探すので `--test-threads=1` が要る）。**組み立ての知識は `dialog.rs` の 1 か所だけ**に置く（`prompt.rs` と `progress.rs` に分かれると、境界の詰め方を片方だけ直す事故になる）。**最初のタブストップは書いた順で決まり、位置ではない** — 要約ダイアログで「閉じる」を先に書いてあるのは、閉じたつもりの Enter でコピーが走らないようにするため。
- **`:delay` の待ち時間は `SetTimer` で刻む**: `thread::sleep` で眠るとメッセージを汲めなくなり、中止ボタンも再描画も効かない。`progress.rs` のダイアログのモーダルループが待ち時間の実体で、`WM_TIMER` のたびに 1 つ起動する。**`WM_TIMER` は低優先度で合体もされる**ので、間隔は「ちょうど N ミリ秒」ではなく「最低でも N ミリ秒」になる（起動の競合を避けるという目的にはそれでよい）。下限を 10 ミリ秒にしているのは `SetTimer` がそれ未満を切り上げるから（書いた値と実際を食い違わせない）。上限の 10 秒は書き間違いの歯止めで、これが無いと無表示のまま何分もぶら下がる。**進行状況を出すかどうかの判定は `launch::shows_progress` の 1 か所**で、`--preview` も同じ関数を呼ぶ（プレビューが「表示します」と言ったのに出ない設定を作らないため）。しきい値より短いときは `thread::sleep` の経路を通るが、1 秒未満なので無表示でも問題にならない。
- **`:delay` は起動をずらすだけで同時実行数を制限しない**: ドキュメントでもそう書く。負荷を抑える機能だと読ませると「効かない」と言われる。効くのは単一インスタンス型アプリの受け渡しの競合とウィンドウの重なり順。**並列数の上限は入れない**（起動したプロセスを管理し続けることになる）。逐次実行は `:wait` として入れた（下記）。
- **`:wait` は「起動したプロセスが終わるまで次を起動しない」**: 待ちの実体は `:delay` と同じ進行状況ダイアログのモーダルループで、タイマーが**起動の合図ではなく様子見の合図**になる（`progress::Step::Busy` を返せば番号が進まない）。終了の判定は `launch::launch_all` が持ち、`progress.rs` は起動も終了判定も知らない（組み立てを `resolve_invocations` の 1 か所に集めるという約束のため）。
  - **「一発実行・常駐しない」とは両立する**。生存時間が延びるだけで、ダイアログが閉じれば終わり、次回起動のために何も残さない。ただし**生存時間の上限が無くなる**ので、`shows_progress` は `:wait` なら 2 件以上で必ず true を返す。中止と一時停止の手立てを常に持たせるのが、上限を置かないことの引き換え。ここを緩めるならこの理由ごと見直すこと。**ダイアログを組み立てられなかったとき（`Outcome::Failed`）は 1 つも起動しない** — 引き換えを果たせないなら実行しない方に倒す。`:delay` だけなら待ち時間に上限があるので、従来どおり無表示で順に起動する。
  - **効く条件は「起動されたプロセスが仕事を終えるまで生きているか」で、時間の長短ではない。** ユーザーが操作して閉じる GUI アプリは**効く側**（10 分開いていてもプロセスは生きている）。効かないのは、起動プロセスが即終了して処理を別プロセスに渡すもの（単一インスタンス型・`.lnk`）。ドキュメントでこの軸を崩さないこと（「終了しないアプリでは効かない」と書くと、手作業を挟む用途を封じる嘘になる）。**`cmd /c` を挟んでも引き継ぎ型は待てない** — 回避策はアプリ側の `--wait` 相当のオプションだけ。
  - **手作業のキュー（開く → 手で直す → 閉じると次が開く）は目玉の用途**であって、注意書きではない。`:delay` でも `+` でも代われない。
  - **ハンドルを持つのは `:wait` のときだけ**（`spawn_command` の `keep`）。常に持つと `:admin` の `SEE_MASK_NOCLOSEPROCESS` を閉じる責任が全経路に増える。判定できないときは「動いていない」に倒す（待ち続けて全体が止まる方が害が大きい）。
  - **値は取らない**（`:admin` と同じ）。`:wait 5000` を黙って受けると、隣の `:delay 300`（ミリ秒）から「5 秒まで待つ」と読んだ人が気づけない。**`[extrun]` の既定値も持たない** — 全体に効かせると、複数選んだだけで終了を待ち続ける項目が意図せず増える。
- **項目名を題名に添えるのは進行状況ダイアログだけ**: `dialog::title_for` が `項目名 ― ExtRun` を組み立てる。**項目名が先**なのはタスクバーのボタンが右から切り詰められるため（区別に効く方を左に置く）。入力欄と要約は一瞬で消えるので素の `ExtRun` のまま。自動で組み立てるのは、`:wait` の任意題名（`:wait 題名`）を却下した判断とセット — 項目名はユーザーが押した文字列そのもので、これ以上に適切な題名はまず書けない。任意指定が要るようになったら `:wait` の引数ではなく独立した `:title` にすること。
- **中止できるのは未起動のぶんだけ**: ExtRun は起動したプロセスのハンドルを持たないので、起動済みは止まらない。UAC を断ったときの `Launch::Cancelled` と同じ意味で、実装も `launch_all` の同じ経路に集めてある。取り違えが起きるのは押す瞬間なので、**ボタン名（「以降の起動を取り消す」）と本文の両方で押す前に伝える**。要約ダイアログはその後始末で、押したあとの説明ではない。
- **残りをファイルに書き出さない**: 「実行中にファイルを書き出さない」という約束を保つのは、任意のコマンドを実行する道具として監査しやすさに実利があるため。加えて**書ける場所を ExtRun が決められない**（exe の場所は書き込めるとは限らず、`%TEMP%` は見つけられず、`%LOCALAPPDATA%` は消す手段が無い＝「exe を消せば完全にアンインストール」が壊れる）。決められない規則は作らず利用者に残す、という `.ps1` の自動インタプリタ回しをやめた判断と同じ筋で、クリップボードに渡して保存の可否を使う人に委ねる。ログ機能の話が出たらここを読み直すこと。
- **件数の確認が数えるのは起動するプロセスの数**: `confirm-over` の判定は `Config::confirm_over_of` の 1 か所で、`menu.rs`（`MenuAction::ExecuteApp` に載せる）と `preview.rs` の両方がここを通る（`delay_of` と同じ理由）。**`+`（まとめて渡す）は何件でも 1 プロセスなので聞かない** — 実害（プロセスが並ぶ・ウィンドウが何十枚も開く）が出るのは起動の回数の方で、`repeated_elevation` が `all_mode` を除くのと同じ判断。**既定を有効（20）にしてある**のは、この機能が守る相手が「件数を意識していない人」だから。書いた人だけが守られる安全装置は、うっかりミスをまさに防ぎたい場面で効かない。無効は `off` で、`0` は「0 件を超えたら」＝常に確認（`0` を無効の意味にすると字面と逆になる）。`delay` が `off` も受けるのはこの語と揃えるためで、意味は `0` と同じ。
- **確認ダイアログは 1 枚にまとめる**: 理由は 3 つある（`:confirm` が書いてある / 件数が多い / `:admin` で UAC が繰り返される）が、聞くのは 1 回で理由を本文に並べる。対象の数だけ聞かれても答えが変わらないのと同じ理屈。組み立ては `confirm::confirm_body` の 1 か所で、**表示（`MessageBoxW`）から切り離してある**のは理由が重なったときの本文をテストで確かめるため（モーダルを出すと触れない）。**件数が理由のときは設定名と現在値を本文に書く** — `:confirm` と違って書いた覚えのない確認なので、これが無いとうるさいと思った人が止め方にたどり着けない。
- **アイコンに `MF_OWNERDRAW` は要らない**: `SetMenuItemInfoW` に `MIIM_BITMAP` で 32bpp のビットマップを渡せば、Vista 以降のメニューがアルファ込みで描く（配色もテーマもシステムのまま）。作り方は `icon.rs` で、**透明な黒で埋めた上下反転なしの DIB に `DrawIconEx` で描く**のが肝。合成結果が `色 × アルファ` になり、メニューが求める形とそのまま一致する（`GetIconInfo` の `hbmColor` を直接渡すと黒い箱になる）。この前提はテストで読み戻して確かめてある。**アイコンは位置で指定する**（サブメニューの親には ID が無いため）。**Per-Monitor V2 でも渡したビットマップは自動で拡大されない**ので、出す先のモニタの DPI から大きさを自分で決める。そのためメニューの位置を項目より先に確定させている。
- **`icons` の既定は `specified`**: `:icon` を 1 つも書いていない設定でアイコンの読み込みが一切走らず、起動時間が変わらないため。コストの読めない `auto` だけをオプトインにする。`IconCache` がパスと番号で覚え、**読めなかったことも覚える**（無いファイルを項目の数だけ叩かない）。
- **環境変数はパス欄だけ・パース時だけ**: `text::expand_env` が `%NAME%` を展開する。対象は実行ファイル欄 / `:dir` / `:icon` の 3 か所で、**引数欄では展開しない**（`cmd.exe /c` の `%errorlevel%` やバッチの `%~n1` を横取りするため）。この線引きのおかげで `%` を `SPECIALS` に足さずに済んでいる。**実行時ではなくパース時に展開する**のは、実行時にすると `100%OFF.png` のような対象ファイル名の `%` まで置換されるから（ユーザーのデータを設定として解釈しない）。未定義はそのまま残す（`--check` の「実行ファイルが見つかりません」に展開前の姿が出るので、専用の警告は置かない）。`ExpandEnvironmentStringsW` ではなく `std::env::var_os` と自前の走査を使うのは、`windows-sys` の feature 追加も 2 回呼びも要らないため。**`:dir` に差し込む値だけ `^` を二重化する**（この欄は `^` を残したまま実行時に解決するので、展開値の中の `^` がエスケープの目印として食われる）。
- **エスケープの処理段階**: 名前は**パース時に** `unescape_name()`、パスは `unescape()` で解決する。引数・作業フォルダ・`:confirm` のメッセージは `^` を**残したまま**保持し、`placeholder.rs` の `replace()` が左から 1 回走査して `^X` → `X` と `$x` → 置換値 を同時に処理する。引数を先にエスケープ解決すると `^$` が `$` になったあとプレースホルダーとして拾われてしまう。`SPECIALS` は `text.rs` の 1 か所だけに置き、`config.rs` と `placeholder.rs` はそれを use する（かつて 2 か所に重複定義されていて、片方だけ直すと `^X` が実行時に素通りする事故になった）。**エスケープの決まりを `text.rs` に集めてあるのはこの事故の再発を防ぐため**で、パース側と実行時側のどちらかに寄せ直さないこと。
- **アクセスキーは名前欄だけの記法**: `MenuItem::name` は**表示用の文字列**で、アクセスキーの `&` は含まない（`^&` は素の `&` に解決済み）。位置は `MenuItem::accesskey`（`name` のバイト位置）が持ち、Win32 に渡すラベルは `menu.rs` の `to_label_wide()` が組み立て直す。**このとき表示名の中の `&` を `&&` に二重化する**（Win32 のメニューは `&` をアクセスキーの目印として食べるため）。`unescape_name()` が 1 パスでエスケープ解決とキーの読み取りを兼ねているのは、先に `^&` を `&` にするとそれがキーの目印として拾われるから。引数欄の `&` は PowerShell の呼び出し演算子として素通しする。
- **アクセスキーのスコープ**: Win32 のニーモニックはポップアップごとにスコープされるので、`check.rs` の `warn_duplicate_accesskeys` が比べるのは兄弟だけ。親と子で同じ文字を使ってよい。ただし**セクションが違ってもルートの項目は同じ階層**に並び（`[.png]` の項目と `[file]` の項目は PNG を 1 つ選んだだけで同居する）、ルートのキーは設定ファイル全体で一意にする必要がある（サンプル設定の不変条件）。
- **プレースホルダーの解釈順序**: `replace()` は `$` の直後を見て `-p` を先に判定する。この判定を崩すと `$-p` が `$-` + フルパスになる。
- **`:when` は拡張子と同じ「出すかどうか」の条件**: だから `filter.rs` で拡張子と一緒に落とし、サブメニューの親（中身ごと消える）にもセパレーターにも同じように効く。`check.rs` の `warn_unreachable_fields`（実行されない項目への `:confirm` などを警告する）の対象には**入れない** — あれは「実行のときだけ効くもの」を並べた表で、`:when` は表示のときに効くため。**値は `single` / `multi` の閉じた集合**（`:when 3` を許すと「3 個以上」「2〜5 個」まで求められて際限がない）。書き間違いは黙って無視せずエラーにする（無視すると意図しない項目がメニューに並ぶ）。
- **複数の対象に対するフィルタは積集合**: 選んだもの全部に当てはまる項目だけを残す（`filter.rs` の `is_menu_item_applicable`）。**理由は表示の量ではなく実行の正しさ** — `resolve_invocations` は項目を選んだ対象すべてに対して起動するので、和集合で出すと「JPEG に変換」が動画にも掛かる。1.7.0 までは和集合だった（意図しないアプリにファイルが渡る不具合）。**「表示されている項目は選んだ全部に使える」は破らないこと。** 和集合に戻して実行側で対象を選り分ける案は、黙って一部だけ処理される（`$i` / `$c` も数が合わなくなる）ので採らない。判定は種類（`file_type`）だけで決まるので `TargetInfo::file_types` の重複を潰した集合を見れば足り、同じ拡張子を何個選んでも判定は 1 回。
- **0 件の案内は `filter::empty_menu_message` の 1 か所**: `menu.rs` と `preview.rs` の両方が使う。**種類が 2 つ以上なら絞り込みの規則そのものを書く** — 1 種類ずつなら出ていた項目が消えているので、規則を知らないと設定ファイルの不備に見える。
- **拡張子の解決はパース時に完了する**: セクション見出しの既定値 → 親項目 → 子項目、の順で継承し、`[+.svg]`（足し算）・`[-.jpg]`（引き算）・`[.svg]`（完全置換）を解決した最終形が `MenuItem::extensions` に入る。`filter.rs` のフィルタは自分の `extensions` だけを見ればよい。
- **`:delay` の既定値の合成は `Config::delay_of` の 1 か所**: 拡張子と違ってパース時に項目へ焼き込まないのは、`[extrun]` がファイルのどこに書いてもよく、項目を作る時点では確定しないため。`menu.rs`（`MenuAction::ExecuteApp` に載せる）と `preview.rs` の両方がこの関数を通る。
- **`+` と `-` は向き以外まったく同じ扱い**: 検証・小文字化・エスケープ（`^+` / `^-`）・セクション見出しでの禁止・完全置換との混在の禁止をすべて共有し、`parse_extensions` の 1 か所で分岐する。同じ拡張子に両方を書いた場合は**どちらを勝たせるかを決めずにエラーにする**（順序で結果が変わる規則を作らないため、`--check` で拾える形に倒した）。重複した指定は `push_unique` で潰すので、`[+.txt]` を継承済みの拡張子に書いても増えない（`-` の空振りが無害なのと対になる）。**別名は展開後に空白で区切られるため符号は各要素に付ける**（`[+@画像]` は `[+.png .jpg]` になり混在エラー）。
- **拡張子の先頭ドット**: 設定側は `.txt` と書く必要がある（`file` / `folder` だけが例外）。`--check` が検出する。設定側も `to_lowercase()` してから比較する。
- **セパレーターも拡張子を持つ**: `--- [folder @画像]` のように書け、セクションの既定値も継承する。フィルタで普通の項目と同じように落ちる。
- **セパレーターの整理**: `cleanup_separators` がフィルタ後に先頭・末尾・連続する `---` を取り除く。フィルタリングを変更するときはここも合わせて確認する。期待される項目数はテストに書いてある。
- **`all_mode`（`+`）**: 1 プロセスに全パスを渡す（引数中の `$p` の位置に展開、`$p` を含まなければ末尾に追加）。既定はファイルごとに別プロセス。展開されるのは**引数がちょうど `$p` のときだけ**で、`-i$p` のように埋め込まれた場合は `targets[0]` を基準に置換されるだけ。取りこぼしに気づけないので `check.rs` の `warn_embedded_path_placeholder` が警告する。**引数欄が空（`MenuItem::args` が空）なら引数なしで起動し、パスを足さない** — 欄を空にするのと欄ごと省略するのは仕様書が区別しており、`+` だけ例外にすると同じ書き方が場所によって逆の意味になる。
- **名前付きフィールドは 1 項目に 1 つだけ**: 重複はエラー（`:admin` / `:wait` も含む）。**後ろが勝つ規則を作らない**のは、順序で結果が変わる規則を増やさないためと、直したつもりで古い行を消し忘れた事故が「直したのに効かない」という形で出るのを防ぐため。別名の定義と `[extrun]` の設定をエラーにしてあるのと同じ判断。**値が要るのは `:dir` と `:icon`**（キーワードだけならエラー）で、`:confirm` の値なしは意図的な仕様なので対象外。
- **`:admin` は起動 API だけを差し替える**: 組み立ては `resolve_invocations` のまま（`Invocation::admin` を運ぶ）で、`spawn_command` が `ShellExecuteExW` の `runas` に分岐する。**`join_args` が引数を 1 本の文字列に組み直す**のがこの経路だけの仕事で、規則は受け取り側の `CommandLineToArgvW` に合わせる（`Command` と同じ）。ここを間違えると空白入りのパスが 2 つに割れるのでテストで固めてある。**`SEE_MASK_NOASYNC` は外せない**（起動したらすぐ終了するプロセスなので、付けないと起動処理ごと打ち切られうる）。COM の初期化は `:admin` を使う設定でだけ払うコストにするため `Once` で遅らせる。UAC のキャンセルは失敗ではないので `Launch::Cancelled` として扱い、**残りの対象も起動しない**。昇格はプロセスごとなので個別実行では UAC が対象の数だけ出る。これは技術的に避けられないため、`confirm_repeated_elevation` が回数を先に知らせる（`+` なら 1 回なので聞かない）。
- **区切りを含まない実行ファイル名は PATH に任せる**: `notepad.exe` のように書かれたパスは `CreateProcess` が PATH から探すので、**起動する前に `exists()` で確かめない**（`launch::searched_on_path` が判定する）。確かめるとカレントフォルダ基準で解決され、PATH にあるものまで撥ねてしまう。`--check` が相対パスの存在を確認しないのと同じ線引きで、片方だけ厳しくすると「`--check` は通るのに起動できない」がまた生まれる。見つからなければ `spawn()` が失敗し、いつもの起動失敗ダイアログが出る。**`Path::parent()` はこの書き方で `None` ではなく空文字列を返す**ので、作業フォルダに素通しすると `CreateProcess` がエラー 123 で落ちる（`resolve_working_dir` が `.` に倒す）。
- **`step` への再入を防ぐ**: `:admin` の起動は `ShellExecuteExW` に入り、UAC の確認が出ているあいだに Windows がメッセージを処理することがある。溜まっていた `WM_TIMER` がそこで配送されると、**番号が進む前に**もう一度 `step` が呼ばれて同じ対象を二重に起動する（`:wait` の「動いているか」の見張りは、起動の直前にハンドルを手放しているので効かない）。`progress.rs` の `LAUNCHING` で塞いである。**確かめるのは `ProgressData` を可変で借りるより前**（借りたあとだと、同じデータへの可変参照が 2 つ同時に存在することになる）。
- **起動失敗の報告**: `spawn()` の失敗はエラーダイアログにする（握りつぶさない）。個別実行では対象の数だけ同じ失敗が並ぶので、同じ理由はまとめて 1 回だけ出す。`.ps1` などを直接指定した場合はインタプリタ経由にする案内を足す（`CreateProcess` は起動できない）。
- **`.bat` / `.cmd` は `needs_interpreter` の表に入れない**: 標準ライブラリが `CreateProcess` を呼ぶ前にこの 2 つだけを `cmd.exe /e:ON /v:OFF /d /c` に差し替えるので、実行ファイル欄に直接書いて動く（`std::sys::process::windows` の `is_batch_file`。引数のエスケープが整ったのは CVE-2024-24576 を直した 1.77.2 で、`Cargo.toml` の `rust-version` はこれを下回ってはいけない。現在の下限は edition 2024 に合わせた 1.85.0 なので条件は満たしているが、**下げる話が出たらこの制約を思い出すこと**）。1.3.0 でこの 2 つを表に入れてしまい、正しく動いている設定に `--check` が誤警告を出した。**`.ps1` を同じように自動で `powershell.exe` に回すのは意図的にやらない** — 実行ポリシーの既定が `Restricted` で、迂回には `-ExecutionPolicy Bypass` を黙って付けるほかなく（グループポリシーは迂回できないので管理端末では失敗する）、しかも `spawn()` 自体は成功するため**エラーダイアログが出ず何も起きないように見える**。`powershell.exe` と `pwsh.exe` のどちらを指すかもウィンドウの出し方も決められない。決められない規則は作らず利用者に残す、という `+`/`-` の混在をエラーにした判断と同じ筋。
- **`working_dir` のプレースホルダー**は `targets[0]` を基準に解決される。未指定時は実行ファイルの親ディレクトリ。
- **`[extrun]` は予約セクション名**: 拡張子は `.` で始まる必要がある（`file` / `folder` だけが例外）ので、この名前は元々エラーになる書き方だった。`build_menu` が見出しを見てモードを切り替え、以降の行を項目ではなく `名前 = 値` として読む。次の拡張子セクションで項目に戻る。**拡張子セクションと違い位置に依存しない**（どこに書いても全体に効く）。
- **表示位置の値の解釈は `config::parse_menu_position` の 1 か所だけ**: 設定ファイルの `menu-position` と `--at` の両方から呼ぶ。片方にだけ書き方を足すとずれる。
- **メニューにキー入力を差し込むには `SendInput` が要る**: `PostMessageW` でオーナーウィンドウに `WM_KEYDOWN` を投げても**メニューには届かない**（`window_proc` に配送されて `DefWindowProcW` に捨てられる）。メニューは自分のモーダルループで本物のキー入力を読むため。`select-first` は `TrackPopupMenu` の前に `SetTimer` を仕掛け、モーダルループが手を空けたところで届く `WM_TIMER` の中から `SendInput` する。タイマーは `select-first` が有効なときしか仕掛けない。
- **`GetForegroundWindow` は `SetForegroundWindow` より前に取る**: `create_and_show_menu` の先頭で捕まえている。自分が前面に出たあとに取ると自分自身になり、`--at window` が壊れる。**最小化されたウィンドウの矩形は `-32000` 付近を返す**ので `IsIconic` で弾かないと画面外にメニューが出る。基準にできないときは画面中央 → カーソル位置の順に落とす。
- **DPI 対応はウィンドウを作る前に済ませる**: `main()` の先頭の `enable_dpi_awareness()`（Per-Monitor V2）が唯一の宣言箇所。マニフェストではなく API で宣言しているので、**この呼び出しより前にウィンドウや DPI 依存の API を触ってはいけない**。宣言しないと座標が仮想化され、`GetCursorPos` などが物理ピクセルを返さなくなるため、位置計算のコードはこれが効いている前提で書く。V2 でなければならないのは、メニューの自動スケーリングが V2 で追加された機能だから。
- **`--check` のコンソール出力**: `WriteConsoleW` を先に試し、失敗したらリダイレクト先とみなして `WriteFile` で UTF-8 を書く。`GetConsoleMode` はハンドルのアクセス権に左右されるため判定に使えない（PowerShell から起動すると失敗し、CP932 で文字化けする）。

## テスト

- `config.rs` — パーサの各規則とエラー検出。
- `text.rs` — エスケープと引数の分解（`split_args` が中括弧をどう数えるか）、`%NAME%` の展開、BOM の読み飛ばし。**パーサを通さず関数を直接呼ぶ**ので、`config.rs` 側のテスト（`parse()` を通す）とは役割が違う。
- `datetime.rs` — `$t{...}` の書式ごとの出力と、`--check` が出すエラーの検出。
- `placeholder.rs` — エスケープとプレースホルダーの相互作用（日時の書式そのものは `datetime.rs` の担当で、ここで見るのはパスやエスケープと同居させたときの振る舞い）。
- `prompt.rs` — `$?{...}` の書式（説明と既定値の分け方、中括弧の対応、`--check` のエラー）。ダイアログ本体は `#[ignore]` 付きのテストで実機のときだけ確かめる。
- `progress.rs` — 進行状況ダイアログの一連の操作（完走・中止・一時停止と再開・起動側からの打ち切り・`Busy` を挟んだ進行・待っている最中の中止・要約の 2 種類）を、`#[ignore]` 付きのテストで実機のときだけ確かめる。`prompt.rs` と同じく題名でウィンドウを探すので、`--test-threads=1` で走らせること。**題名は `dialog::title_for` で組み立てたものを探す**（素の `ExtRun` では進行状況ダイアログは見つからない）。
- `launch.rs` — 進行状況を出すしきい値（`shows_progress`）、中止したときに残るパスの範囲（`remaining_paths`）、`launch_in_order` が `Busy` と `Stop` をどう扱うか。**終了の判定は実プロセスで確かめる**（`cmd /c ping` を起動して `Running::alive` の移り変わりを見る）。ダイアログ経由の逐次起動は `#[ignore]` 付きで、**かかった時間**で重なっていないことを判定する（3 プロセスで 2 秒以上。重ねて起動していれば 1 つぶんで終わる）。
- `dialog.rs` — `title_for` の組み立て（項目名が先・空なら素の題名・長い名前の丸め）。
- `confirm.rs` — `confirm_body` が組み立てる本文（理由が重なったとき・見出しの選び方・一覧の打ち切り）。判定そのものは `config.rs` の `confirm_over_of` の側で見る（境界・`+`・`off`）。
- `preview.rs` — `--preview` の整形。時刻を固定した `RunContext` を渡して突き合わせる。
- `icon.rs` — 取り出したビットマップの大きさと、アルファを掛けたあとの色になっているか（`色 ≤ アルファ` を読み戻して検査する）。
- `menu.rs` — `icons` の 3 つのモードで項目にビットマップが付くかを、`GetMenuItemInfoW` で読み戻して確かめる（見た目は検査できないが、付いたかどうかは分かる）。表示名とアクセスキーからラベルを組み立て直す `to_label_wide` もここ。
- `icon.rs` の `cache_tests` — `IconCache` が同じパスと番号を使い回すこと、**読めなかったことも覚える**こと（無いファイルを項目の数だけ叩かない）。
- `invoke.rs` — `:admin` が組み立てに伝わるか。
- `check.rs` — 実行ファイルのパスと `+` の `$p` の書き方、アクセスキーの重複、届かない `:confirm`、取り出せない `:icon` に対する警告。
- `main.rs` — `take_options` によるコマンドライン引数の切り出し（`--at` / `--select-first` / `--no-select-first`）。`Overrides` は `Option` で「指定なし」と「明示的に no」を区別する。
- `filter.rs` — **`tests/fixtures/full-config.txt` をフィクスチャとして**、20 種類の対象について構築されるメニューの項目数と並び順を突き合わせる。フィクスチャを編集すると落ちるので、そのときは期待値も合わせて更新する（期待値を一度 `0` にして走らせると、失敗メッセージに実際の数が並ぶ）。**同梱サンプル（`extrun-config.txt`）も別のテストが読み**、書式エラーが無いことと項目数が増えていないことを見張る。

`cargo test` は 3 つのターゲットを走らせる。**ライブラリ（大半のテストはここ）／バイナリ（`main.rs` の引数の切り出しだけ）／doctest**。どれかだけを見るときは `--lib` / `--bin extrun` / `--doc` を付ける。

テストコードは `cargo test` でのみコンパイルされ、リリースバイナリのサイズに影響しない。遠慮なく書いてよい。

## 設計方針

- **起動速度が最優先**。ただし体感に影響しない最適化のためにコードを複雑にしない。パース速度も起動時間の誤差の範囲なので、可読性を優先する。
- **バイナリサイズの削減**が第二の目標（`[profile.release]` は `opt-level = "z"` / `lto = "fat"` / `codegen-units = 1` / `strip` / `panic = "abort"`）。
- **実行時の依存クレートは `windows-sys` のみ。増やさない。** ビルド時の依存は VERSIONINFO の埋め込みに使う `embed-resource` だけで、バイナリには入らない。こちらも増やさない。feature の追加は「クレートを増やさない」に反しないが、要る理由を `Cargo.toml` のコメントに書くこと（`Win32_System_Threading` は `:wait` で `:admin` のプロセスの終了を見るためだけに足した）。
- 旧 YAML 形式との互換は不要（`extrun.yaml` の読み込みコードは残さない）。テーマ機能は削除済み（`TrackPopupMenu` の配色を変えるには `MF_OWNERDRAW` の自前実装が必要なため）。

## extrun-make（設定づくり）

設定ファイルの数行を組み立てて渡すだけの 2 つ目の実行ファイル。**編集はしない** — 生成 → 表示 → クリップボード（必要なら末尾に追記まで）。既存の設定ファイルに書き戻さないのは、コメントや整形を壊さないため。友人に使ってもらったときの詰まりどころ（コマンドラインに明るくない / 設定できることが多い / 動きが想像できない / 設定ファイルを書き慣れていない）への答えとして足した。

`src/bin/extrun-make/` に置き、`form.rs`（組み立て）/ `layout.rs`（画面の寸法）/ `presets.rs`（ひな型）/ `clip.rs`（クリップボードとファイル選択）/ `main.rs`（ダイアログ手続き）に分かれる。

- **ツールは必須にしない。** 設定ファイルは手で書けるまま。ツールで作れない項目は手で書けばよい、という関係を保つ。`readme.txt` にもそう書く。
- **組み立ては `form.rs` の `to_config` の 1 か所だけ。** 画面の側に書くとテストから触れなくなり、そのまま「動かしてみないと分からない」ものになる。`main.rs` は `Form` を読み書きする入れ物に徹する。
- **正しさは字面ではなくパーサを通した往復で見る。** 組み立てた文字列を `config::parse` に通して名前やパスが元に戻るかを確かめるテストがある。エスケープの対象を増やしても減らしても、ここが通っていれば事故は起きない。逆にここを消すと、`text::escape_name` の判断を目視で議論することになる。
- **エスケープは `text::escape_name` / `escape_path` を使う。自前で書かない。** `SPECIALS` が 2 実装に分かれる事故を、プロセスをまたいで再現することになる。掛けるのは**その欄で意味を持つ文字だけ**で、全部を潰すと「C++ で開く」が `C^+^+ で開く` になり、貼る人が読めない（設定を書き慣れていない人のための道具なので本末転倒）。
- **3 列なのは縦を押さえるため。** 2 段組みで縦に積むと、実測で 1920×1080 の 125%（15 型ノートの既定によくある組み合わせ）でも詳細設定を開いた瞬間に画面からはみ出す。1 列あたりの幅は 2 段組みとほぼ同じ（490 dlu を 2 分割 ≒ 700 dlu を 3 分割）なので欄は狭くなっていない。**詳細設定は下に伸ばす** — 横に 4 列目として開くと幅が 920 dlu になり、100% の 1366 幅と 150% の 1920 幅で今度は横が溢れる。
- **開閉でコントロールを動かさない。** 詳細設定は最初から下端に置いてあるものを `ShowWindow` で見せ隠しし、ダイアログの高さだけを付け替える。位置が変わるのは下の帯の 2 つ（説明と「閉じる」）だけ。全部を並べ直すと、開閉のたびにレイアウトがずれる余地ができる。
- **画面からはみ出すなら ④ の欄を縮める。** 1920×1080 の 150% のように「その画面にしては拡大率が大きい」組み合わせでは、開いた状態が作業領域を 60〜80 px 超える。`SPI_GETWORKAREA` と突き合わせて、**伸縮してよいのは ④ だけ**（フォームの行を縮めると読めなくなる）。
- **引数欄を空にするのと欄ごと省くのは別の意味**（前者は引数なし、後者は `$p` が渡る）。仕様がそう区別しているので、チェックボックス（`no_args`）で書き分けられるようにしてある。片方しか出せないと、同梱サンプルの `cmd.exe |` が作れない。
- **プレースホルダーの挿入はボタンを押したときだけ。** `CBS_DROPDOWNLIST` は矢印キーで一覧を辿るたびに `CBN_SELCHANGE` を飛ばすので、選択と同時に挿し込む作りだと途中のものが全部入る。
- **引数欄の下に出すのは「いま書かれているものの意味」だけ。** 早見表を常設すると場所を食ううえ初めての人には量が多い。打ち間違えると何も出ないので、そこでも気づける。
- **ひな型（対象の種類）は `extrun-make` 自身が持つ固定の表。** 設定ファイルが空でも初回起動でも必ず使える必要がある。並びは「自分で指定 → （Phase 5 で別名）→ ひな型」で、書く回数の多い順。**選んだものがそのまま書かれる**という 1 本の規則にしてあり、ひな型を選べば展開した拡張子が、別名を選べば `[@画像]` が書き出される。
- **`dialog.rs` を `pub` にしてあるのはこのツールのため。** 組み立ての知識を 2 か所に分けない（`lib.rs` の冒頭に、開けたものと理由が書いてある）。

## 関連ドキュメント

- **`docs/extrun-config-format.md`** — 設定ファイル形式の完全仕様。パーサに手を入れるときの正典。
- **`extrun-config.txt`** — 配布物に同梱する公開用のサンプル設定。**初めて開く人が読み通せる最小限であることが不変条件**（1.9.0 で決定）。友人に試してもらったところ「サンプルの中身に圧倒された」という声が返ってきたのが直接のきっかけで、**書式を網羅する役目はここから外した**。PowerShell の一行野郎を置かないこと（`^$` のエスケープは書式ではなく PowerShell と ExtRun の衝突を吸収する記法で、初日に見せるものではない）。書き方の実例を足したくなったら `docs/extrun-recipes.md` の 3 章へ。
- **`tests/fixtures/full-config.txt`** — 書式のほぼすべてを一通り使ったテスト用フィクスチャ。**配布物には入らない**（かつては同梱サンプルそのものだった）。`filter.rs` のメニュー構造テストが読むので、編集したら期待値も直す。
- 上の 2 つと `docs/extrun-recipes.md` に共通する決まり — **`--check` が `問題は見つかりませんでした` にならなければいけない**（レシピ集は各コードブロックを繋げた形で検証する）。項目を足すときは次に注意する。
  - パスは素の Windows 10/11 に必ず存在するものだけ（`mspaint.exe` と WordPad は Windows 11 には無い）。実機で `Test-Path` を確認してから書く。**レシピ集の 4 章以降（外部アプリ）はこの限りではない。**
  - PowerShell に渡すコマンドは**全体を `"..."` で囲んで 1 引数にする**。分割すると空白入りパスで壊れる。中では `'...'` を使う。
  - `@別名` の直後に `'` を置かない（引用符は名前の終端にならないので `tar'` を探しに行く）。`@sys\tar.exe` のように `\` を挟む。
  - ユーザーのファイルを書き換える項目は避け、新しいファイルを作る形にする。
  - アクセスキーを足すときは**ルート階層だけセクションをまたいで一意**にする（サブメニューの中は独立）。`--check` が重複を検出する。`(&O)` を末尾に足すと表示名が変わるので `filter.rs` の項目名テストも直す（`&PNG` のように名前の中の文字に付ける場合は変わらない）。
  - パーサを通した最終的な引数は、`filter_menu_items` → `PathPlaceholders::replace_args` を呼ぶ一時テストでダンプして、`ProcessStartInfo.ArgumentList`（Rust の `Command` と同じクォート規則）で実行して確かめられる。
- **`docs/extrun-recipes.md`** — 設定例集。**3 章は Windows 標準コマンドだけで書き、追加インストールなしで動く**（同梱サンプルを最小限に削った代償を引き受ける節で、サンプルの次に読む場所として README・仕様書・readme.txt から案内している）。**4 章以降は外部アプリ**（ffmpeg / ImageMagick / IrfanView / 7-Zip / VS Code / VLC / Pandoc / 画像最適化系）で、各自のインストール先に依存するため想定バージョンを明記してパスは読み替え前提。各レシピに「使用: `$-p` / `[-.mp4]` / `+`」の注記を付けて書式の逆引き教材を兼ねさせているので、レシピを足すときもこの形を守る。付録 A に書式 → レシピの逆引き表、付録 C に AutoHotkey の呼び出し例がある。**章番号を挟み込むと付録 A と目次のアンカーが全部ずれる**ので、番号を変える改変は逆引き表の突き合わせまでやること。なお「元のファイルを書き換えず新しいファイルを作る」という方針はこちらでも守ること。
- **`docs/development.md`** — ビルド環境・依存クレート・プロジェクト構成・リリース手順・使用している Windows API。README から切り出した開発者向けの内容で、**このファイル（CLAUDE.md）と意図的に重なる**。CLAUDE.md は「なぜその実装なのか・壊してはいけない不変条件」を、development.md は「どう動かすか」を担当する。ビルド手順やリリース手順を変えたら両方を見ること。
- **`README.md`** — 「これは何か・どう入れるか・どこを読むか」に徹する。**書式の解説を書き足さない**（仕様書との二重管理になり、実際に腐った）。書けることは一覧表で示して各節へリンクする。
- **`packaging/readme.txt`** — 配布 zip に入れる説明書。`README.md` の要約ではなく、zip を展開した人向けの独立した文書。見出しのバージョンは `Cargo.toml` と揃える（`build-release.ps1` が検査する）。
- **`CHANGELOG.md`** — 変更履歴。Keep a Changelog 形式。リリースのたびに追記する。**配布 zip にも同梱するので、利用者から見て何が変わるかを書く**（リファクタリング・テストの追加・CI・ドキュメントの内部整理は載せない。それは git log の担当）。設定ファイルの見直しが要る変更は必ず「変更」の欄に入れる。利用者はそこだけ読んで更新の可否を判断する。
- **`.github/workflows/`** — `ci.yml`（`fmt` / `clippy -D warnings` / `test` / **`--ignored` の実機テスト** / 同梱サンプルとフィクスチャの `--check`）と `release.yml`（タグから zip を作って下書き Release に添付）。`#[ignore]` 付きのテストも CI で毎回走る（`windows-latest` にはデスクトップがある）。**手で叩く前提のテストはいずれ叩かれなくなる**ので、`--test-threads=1` を付けた専用のステップにしてある。固まったときに備えて `timeout-minutes: 5`（ローカルでは 5 秒で終わる）。

同じ話が README.md・`docs/extrun-config-format.md`・`packaging/readme.txt` の 3 か所に出てくることがある（導入手順、`--check`、トラブルシューティング）。片方だけ直すとずれるので、書き換えるときは横断で確認する。
