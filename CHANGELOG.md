# 変更履歴

新しい版が上にあります。各版の下は次の区分に分かれています。

- **追加** — 新しくできるようになったこと
- **変更** — これまでと動きが変わったこと。**更新する前にここだけ読めば、
  設定ファイルの見直しが要るかどうかが分かります**
- **修正** — 不具合の直ったところ

内部の作りだけを変えた分（リファクタリングやテストの追加など）は、
使ううえで違いが出ないので載せていません。

書き方は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に、
バージョン番号は [セマンティック バージョニング](https://semver.org/lang/ja/) に従います。

## [Unreleased]

### 変更

**この 3 つは、これまで読み込めていた設定ファイルがエラーになることがあります。**
更新後に `extrun.exe --check` を一度通してください。

- **同じ項目に同じ名前付きフィールドを 2 回書くとエラーになります**（`:dir` `:confirm` `:icon` `:delay` `:admin` `:wait`）。これまでは黙って後ろが勝っていました。直したつもりで古い行を消し忘れると「直したのに効かない」という分かりにくい詰まり方をするため、書いた時点で気づけるようにしました。別名の定義と `[extrun]` の設定は以前からエラーです
- **`:dir` に値を書き忘れるとエラーになります。** これまでは黙って未指定として扱われ、実行ファイルの親フォルダが使われていました。値の無い `:icon` は以前からエラーです（`:confirm` だけは値なしが仕様どおりで、これまでどおり書けます）
- **`+`（まとめて渡す）の項目で引数欄を空にした場合、引数なしで起動するようになりました。** これまでは `+` のときだけ、空欄でも末尾に全パスが追加されていました。仕様書は以前から「欄を空にすると引数なし」と書いており、`+` だけが例外になっていたのを揃えたものです。全パスを渡したい場合は**引数欄ごと省略する**か `$p` と書いてください

### 修正

- **`:admin` の確認（UAC）を途中で断ったとき、要約の件数が 1 件合わなくなっていたのを直しました。** 断ったファイルが「起動した数」にも「残りの数」にも入らず、10 件中 2 件起動・残り 7 件、のように 1 件消えていました。**「残りをコピー」で貼り付けた一覧からも漏れていた**ので、やり直したいファイルがちょうど抜け落ちていました
- **実行ファイル欄に `notepad.exe` のような名前だけを書いても起動できるようになりました。** これまでは PATH を探す前に「実行ファイルが見つかりません」で止まっていました（`--check` は相対パスを確認しないので、書いた時点では何も言われませんでした）。あわせて、この書き方のときに作業フォルダが空になって起動に失敗する不具合も直しています
- `:wait` を書いた項目で進行状況ダイアログを表示できなかったとき、**何も表示されないまま中止もできない状態で固まることがあったのを直しました。** この場合は理由を伝えて実行を取りやめます。`:wait` には待ち時間の上限が無いため、中止する手立てを用意できないなら実行しない方に倒しています
- `:admin` を付けた項目で、UAC の確認が出ているあいだに**同じファイルが二重に起動されうる隙間**を塞ぎました
- **`--check` が `:dir` と `:confirm` の書式の誤りを、実際とは違う行番号で報告していたのを直しました。** 3 行目の誤りが「2 行目」と出るなど、項目の行を指していました
- **入力欄の説明に `{` を書けるようになりました。** `$?{PowerShell の {0} 形式で}` のような書き方が、引数の途中で切れたうえ「`$?{` が `}` で閉じられていません」という見当違いのエラーになっていました
- **`+`（まとめて渡す）の引数に `^$` でエスケープした `$` を書くと、対象のパスがどこにも渡らないまま起動していたのを直しました。** `^$path` のような書き方が該当します。`--check` は正しく読めていたため警告も出ず、気づく手がかりがありませんでした

## [1.6.0] - 2026-08-17

### 追加

- 名前付きフィールド **`:wait`** — 複数のファイルを選んだときに、**起動したアプリが終了してから次の 1 つを起動する**。`:delay` が起動をずらすだけなのに対し、こちらは終わるまで次を始めない
  - `:admin` と同じく書くか書かないかだけで、値は取らない（`:wait 5000` はエラー）。全体の既定値は無く、書いた項目でだけ効く
  - **処理が終わると自分で終了するツールだけでなく、開いて手で操作してから閉じるアプリでも効きます。**「1 枚開く → 手で直す → 閉じると次の 1 枚が開く」という使い方ができます
  - **起動したあとすぐ終了して処理を別のプロセスに渡すアプリでは待ちません**（すでに開いているウィンドウに渡す単一インスタンス型のアプリ、ランチャー、`.lnk` 経由の起動など）。そのアプリ自身に「終了まで待つ」オプションがあれば併記してください（VS Code の `--wait` など）
  - 対象が 2 つ以上あれば、待ち時間の長短にかかわらず**進行状況ダイアログが必ず出ます**。終わるまでの時間は起動された側に委ねられていて事前に分からないので、中止と一時停止の手立てを常に用意しています。ExtRun が待つ時間に上限はありません
  - `:delay` と併記すると、終了を待ってからさらに間隔を空ける
  - `+` は何個選んでも 1 プロセスなので `:wait` は効かない。`--check` が警告する。実行されない項目（サブメニューの親・セパレーター）に付けた場合も警告する
  - `--preview` に「起動の順番」の行が出る

### 変更

- **進行状況ダイアログの題名に項目名が入るようになりました**（`PNG に変換 ― ExtRun`）。`:wait` では待っているあいだに別の ExtRun を起動することもあるため、タスクバーで見分けられるようにしたものです。入力欄と要約のダイアログはこれまでどおり `ExtRun` のままです
- **`[extrun]` セクションの中に書いた `:admin` がエラーになります。** これまでは黙って無視されていましたが、`:dir` や `:delay` と同じくエラーとして扱うようになりました。**エラーがあるとメニューは表示されません。** 更新後にメニューが出なくなった場合は `extrun.exe --check` を実行してください。該当の行を消せば直ります（`[extrun]` は `名前 = 値` を書く場所で、`:admin` はもともと効いていません）

## [1.5.0] - 2026-08-16

### 追加

- 名前付きフィールド **`:delay`** — 複数のファイルを選んだときに、1 つずつの起動のあいだへ待ち時間を入れる（ミリ秒）。`[extrun]` の **`delay`** に書けば、`:delay` を書いていない項目の既定値になる
  - 値は `0`（待たない。`off` とも書ける）か `10`〜`10000`。項目に書いた値が既定より優先され、`:delay 0` と書けば既定を打ち消せる
  - **同時に動くプロセスの数は減りません。**ずらして起動するだけで、しばらくすれば結局すべてが同時に動きます。効くのは、2 つ目以降を 1 つ目に渡す作りのアプリ（ビューアやエディタによくあります）で、同時に叩くと取りこぼされてしまう場合です。重い処理の負荷を抑えたいときは `+`（まとめて渡す）で受け側にまとめさせてください
  - `+` は何個選んでも 1 プロセスなので `:delay` は効かない。`--check` が警告する。実行されない項目（サブメニューの親・セパレーター）に付けた場合も警告する
  - `--preview` に「起動の間隔」の行が出る。待ち時間の合計と、進行状況を表示するかどうかが分かる
- **起動の進行状況ダイアログ** — `:delay` の待ち時間の合計が 1 秒以上になるときに出る。何件目まで進んだかが見え、**一時停止**と**以降の起動の取り消し**ができる
  - 一時停止するとその場で待ち時間が止まり、再開すると間隔を待たずにすぐ次が起動する
  - **取り消せるのはまだ起動していないぶんだけで、起動済みのアプリは止まりません。**Esc と × も同じ扱い
  - 途中で止めると、何を起動して何が残ったかの要約が出る。**残りのパスはクリップボードに渡せる**ので、そのまま貼って続きを実行できる。ExtRun 自身はこれまでどおりファイルを書き出さない
- `[extrun]` の **`confirm-over`** — 起動する数がこの件数を超えるときに、`:confirm` を書いていない項目でも実行前の確認を出す。フォルダの中身を全部選んだつもりが 300 個あった、という選び間違いを、プロセスが並び始める前に止められる
  - 値は件数か `off`（確認しない）。`0` は「0 件を超えたら」＝常に確認、という意味になる
  - 数えるのは対象の数ではなく**起動するプロセスの数**。`+`（まとめて渡す）を付けた項目は何個選んでも起動が 1 回なので出ない
  - 確認の本文には対象の一覧が並び、しきい値を超えたことと設定の現在値も出る

### 変更

- **既定で、21 件以上を個別に起動するときに確認ダイアログが出るようになりました。** 上記 `confirm-over` の既定値が `20` のためです。これまでどおり確認なしで起動したい場合は、設定ファイルに次を書いてください

  ```
  [extrun]
  confirm-over = off
  ```

- `:confirm` と `:admin` の「UAC が N 回出ます」の知らせが重なるとき、**ダイアログを 1 枚にまとめる**ようになった。理由が複数あっても聞かれるのは 1 回

## [1.4.0] - 2026-08-16

### 追加

- 名前付きフィールド **`:admin`** — 書いた項目を管理者として起動する。UAC の確認で「はい」を選んだときだけ実行され、キャンセルすると何も起きない
  - 順番は 入力欄 → `:confirm` → UAC → 起動
  - **昇格はプロセスごとにしかできない**ので、ファイルごとに別プロセスで起動する項目に付けると、選んだ数だけ UAC が出る。`+`（まとめて渡す）と組み合わせれば 1 回で済む
  - `+` の付いていない `:admin` を複数の対象に対して選んだときは、起動する前に「確認が N 回表示されます」と 1 回だけ知らせる。UAC を途中でキャンセルすると残りの対象は起動しない
  - `--preview` に「実行の権限」の行が出る。`--check` は、実行されない項目（サブメニューの親・セパレーター）に付いた `:admin` を警告する
- **`--config <パス>`** — 読み込む設定ファイルを指定できるようになった。用途ごとに設定を分けたり、書き換えたものを本番の設定と入れ替えずに試したりできる
  - メニュー表示・`--check`・`--preview` のどれでも使える
  - 相対パスはカレントディレクトリ基準。`--config=パス` の書き方もできる
  - 指定したファイルが無ければエラーになる（既定の設定ファイルへは戻らない）
  - 「送る」に置くショートカットごとに別の `--config` を付ければ、用途別のメニューを並べられる

### 修正

- **実行ファイル欄に `.bat` / `.cmd` を直接書いた項目に、`--check` が誤って警告していた**のをやめた。この 2 つは直接書いて問題なく動く（1.3.0 で追加した警告の対象が広すぎた）。`.ps1` / `.vbs` / `.js` は従来どおり直接起動できないため、警告は続く
  - `.bat` を書いた設定を使っていた場合、動作は変わりません。`--check` の警告が消えるだけです
  - バッチの中で受け取った引数は `set "TARGET=%~1"` のように**引用符で囲んでください**。裸の `%~1` はファイル名に含まれる `&` や `^` で壊れます

## [1.3.0] - 2026-08-15

### 追加

- パスの中の **`%LOCALAPPDATA%` のような環境変数を展開する**ようになった。ユーザー名がパスに入るアプリ（VS Code など）を、設定ファイルを書き換えずに別の PC でも使えるようになる
  - 展開されるのは**パスを書く 3 か所**（実行ファイル欄・`:dir`・`:icon`）だけ。**引数欄では展開しない** — `cmd.exe /c` に渡す `%errorlevel%` やバッチの `%~n1` を横取りしないため
  - 未定義の変数は `%FOO%` のまま残る。`--check` の「実行ファイルが見つかりません」に展開されていない姿で出るので、綴りの間違いはそこで分かる
  - 大文字小文字は区別しない。`%%` や閉じ忘れの `%` はそのまま残るので、`C:\100%\app.exe` のようなパスも書ける
  - 展開は設定ファイルを読む時点で済ませるので、**対象のファイル名に含まれる `%` は展開されない**（`100%OFF.png` はそのまま扱われる）
- 項目ごとの拡張子指定に**足し算 `[+.svg]`** を追加。受け継いだ拡張子に足せるようになった（これまでは引き算 `[-.jpg]` か、全部書き直す完全置換 `[.png .jpg .svg]` の 2 択だった）
  - `[+.svg -.jpg]` のように引き算と混ぜて書ける。どちらも「受け継いだものからの差分」で、向き以外の扱いは同じ
  - すでに受け継いでいる拡張子を足しても重複しない
  - `+` / `-` 付きと符号なし（完全置換）を混ぜること、同じ拡張子に `+` と `-` の両方を書くことはエラー。セクション見出しの中では従来どおり使えない（継承元がないため）
- `--check` が、**実行ファイル欄に `.bat` / `.cmd` / `.ps1` / `.vbs` / `.js` を直接書いた項目**を警告するようになった。これらは直接起動できず（`cmd.exe /c` や `powershell.exe -File` を経由する必要がある）、これまでは実際に選んだときの「起動できませんでした」で初めて分かった
- `--check` が、**引き算で対象の拡張子が空になった項目**を警告するようになった。`[.txt]` のセクションで `[-.txt]` と書くと、対象を外すつもりが「すべての対象で表示される」という正反対の結果になるため（指定が空の項目はどの対象でも出る決まり）

## [1.2.0] - 2026-08-15

### 追加

- `--preview <パス>` — そのパスに対してメニューに出る項目と、実際に起動されるコマンドラインを、起動せずに表示する。`--check` が書式を見るのに対して、こちらはプレースホルダーとエスケープの解決結果を見る
  - 引数は 1 つ 1 行で表示されるので、引用符で囲み忘れて空白のところで割れた引数を見つけられる
  - 複数のパスを渡すと起動の回数が分かる。個別実行の項目は対象の数だけ並び、`+`（まとめて渡す）の項目は 1 回の起動に全パスが並ぶ
  - 設定ファイルにエラーがあるときは終了コード 1 で終わる
- 日時のプレースホルダー `$t{...}`。引数と作業フォルダの中で使え、実行した日時に置き換わる（`$-p_$t{yyyyMMdd}.zip` → `C:\folder\file_20260815.zip`）
  - 書式は `yyyy` `yy`（年）/ `MM` `M`（月）/ `dd` `d`（日）/ `HH` `H`（時）/ `mm` `m`（分）/ `ss` `s`（秒）。**`ddd` `dddd` は日本語の曜日**（`土` / `土曜日`）、`EEE` `EEEE` は英語の曜日（`Sat` / `Saturday`）
  - 英字でない文字はそのまま通るので `$t{yyyy年MM月dd日}` と書ける。書式以外の英字は中括弧の外に出す（`$t{yyyyMMdd}_backup`）
  - 時刻は項目を選んだ時点で 1 回だけ決まる。複数のファイルを選んで個別に起動しても、対象ごとに秒がずれてファイル名がばらけることはない
  - 書式の書き間違いは `--check` がエラーとして報告し、メニューは表示されない
- 名前付きフィールド `:confirm`。書いた項目は実行する前に確認のダイアログを出す
  - 値はメッセージ（省略可。省略すると項目名を使った文面になる）。プレースホルダーと別名が使える
  - ダイアログには**対象の一覧**が添えられるので、選び間違えていないか目で確かめられる
  - **既定のボタンは「いいえ」**。`select-first` を有効にしていても、Enter を続けて誤って実行してしまうことがない
  - 確認は項目に対して 1 回。複数のファイルを選んでも対象の数だけ聞かれることはない
  - サブメニューの親やセパレーターに書くと `--check` が警告する（選んでも実行されないため）
- 入力欄のプレースホルダー `$?{説明=既定値}`。項目を選んだあとに入力欄が出て、入力された文字に置き換わる。解像度や品質を変えたいだけの項目を、値の数だけ並べずに済む
  - 既定値は省略でき、書いた場合は選択された状態で入る（そのまま打てば置き換わり、Enter だけなら既定値が使われる）
  - **同じ内容の `$?{...}` は 1 回しか聞かれない**ので、入力した値を引数の複数の場所で使える
  - キャンセルすると何も実行されない
  - 説明と既定値の中でプレースホルダーと日時が使える（`$?{$n の新しい名前=$a_$t{yyyyMMdd}}`）
  - 順番は入力欄 → `:confirm` の確認 → 起動。確認のメッセージにも `$?{...}` を書けるので、入力した値を確かめてから実行できる
- 入力値の決まり。`$?` と `{` のあいだに書くと、入力できる値を絞れる
  - `$?int{...}` 整数 / `$?num{...}` 数値 / `$?name{...}` ファイル名に使える文字（`$?{...}` は制限なしのまま）
  - **決まりを満たさない入力は打ち切らず、理由を添えて聞き直す。** 打った内容は残る
  - `$?name{...}` は空・使えない文字（`\ / : * ? " < > \|`）・終わりの `.` と空白・予約された装置名（`CON` `NUL` など）・255 文字超（NTFS の名前 1 要素の上限）を弾く
  - `$?int{...}` は全角数字や単位付き（`1280px`）も弾く
  - **既定値も `--check` が検証する**ので、`$?int{品質=たかい}` のような書き間違いは実行前に見つかる
- 名前付きフィールド `:icon`。項目の名前の左にアイコンが出る
  - 値は `パス` または `パス,番号`（`.reg` やショートカットと同じ書き方）。`.ico` のほか、アイコンを持つ `.exe` や `.dll` を指定できる
  - **サブメニューの親にも付く**
  - `--check` は**実際に取り出せるかまで確かめる**。ファイルが無いときも、番号が範囲の外のときも警告する
- グローバル設定 `icons`。アイコンを出すかどうかを 3 段階で切り替える
  - `none` 出さない（`:icon` を書いていても）/ `specified` `:icon` を書いた項目だけ（**既定**）/ `auto` 書いていない項目は実行ファイルから取り出す
  - 既定の `specified` では、`:icon` を 1 つも書いていない設定でアイコンの読み込みが一切走らないので、**起動時間はこれまでと変わらない**
  - `none` は設定を消さずに一時的に止めるためのもの
- 同梱のサンプル設定に「長辺を指定して縮小する」（画像）と「圧縮 > ZIP > 日付をつけて圧縮」を追加、「開く」に `:icon` を追加
- 同梱のサンプル設定の「読み取り専用・隠し属性を解除」に `:confirm` を追加（ファイルを書き換える唯一の項目のため）

### 修正

- **同梱のサンプル設定の「長辺 1280px に縮小する」が正しく動いていなかった。** PowerShell が `[Math]::Min(1, 比率)` を整数の多重定義として解釈して比率を丸めていたため、長辺 2560px 以下では縮小されず、2560px を超えるとエラーで失敗していた。あわせて、**縮小が要らない画像（長辺が 1280px 以下）では何も作らない**ようにした（従来は同じ大きさの PNG ができ、元が JPEG だとファイルサイズがかえって増えていた）
- 引数を空白で区切るときに、`$t{...}` の中括弧の中まで区切っていた。`$t{yyyy-MM-dd HH:mm}` のように空白を含む書式が 2 つの引数に割れていたのを修正

### 変更

- 引数と作業フォルダの中の `$t{` が日時として解釈されるようになった。**`$t{` という文字列をそのまま渡していた場合は `^$t{` と書き換えが必要**。中括弧が続かない `$t` は今までどおりそのまま渡る
- **右クリックメニューへの直接登録（`.reg`）の同梱と案内をやめ、「送る」メニューへの登録に一本化した。** 配布 zip から `registry\` フォルダが無くなる
  - Windows の仕様上、`shell\...\command` に `"%1"` を書く形式の登録には次の制限がある。複数のファイルを選ぶと**ファイルの数だけ ExtRun が起動する**ため `+`（まとめて渡す）を付けた項目が使えず、**16 個以上のファイルを選ぶと右クリックメニューから ExtRun の項目自体が消える**
  - 「送る」にはこの制限がなく、選んだファイルは何個でも 1 つの ExtRun にまとめて渡される。ショートカットを複数置いて `--at` などを使い分けることもできる
  - **すでに `.reg` で登録済みの場合、その登録はそのまま動く。**解除したくなったときは 1.1.0 の zip に入っている `registry\extrun-remove.reg` を使う（手元に無いときの手順は `readme.txt` の 6 章にある）

## [1.1.0] - 2026-08-15

### 追加

- メニュー項目のアクセスキー。名前の中の `&` が次の 1 文字をキーにする（`開く (&O)` / `&PNG に変換`）。キーはメニューごとに独立するので、親と子で同じ文字を使える
- `^&` によるエスケープ（アクセスキーにしない `&`）
- `--check` が同じメニュー内のアクセスキーの重複と、`&` の書き間違い（直後が半角英数字でない、1 項目に 2 つ以上）を警告する
- 同梱のサンプル設定 `extrun-config.txt` の主な項目にアクセスキーを追加
- グローバル設定のセクション `[extrun]`。ファイルのどこに書いても全体に効く
  - `menu-position` — メニューを表示する位置（`cursor` / `window` / `screen` / `X,Y`）
  - `select-first` — 先頭の項目を選択した状態で開くか（`yes` / `no`）
- コマンドライン引数 `--at` / `--select-first` / `--no-select-first`。設定ファイルより優先されるので、右クリック登録とホットキーで同じ設定ファイルのまま挙動を変えられる
- 配布 zip に右クリックメニュー登録用の `registry\extrun-add.reg` / `registry\extrun-remove.reg` を同梱。書き換えるのは `extrun.exe` のパスだけで済む
- 配布 zip にこの変更履歴（`CHANGELOG.md`）を同梱

### 変更

- 引数欄の `^&` がエスケープとして解決されるようになった（以前は `^&` の 2 文字がそのまま渡っていた）。エスケープしていない素の `&` の扱いは変わらないので、PowerShell の呼び出し演算子はそのまま書ける
- `readme.txt` を整理（301 行 → 248 行）。設定の書き方は `extrun-config-format.md` に、レジストリ登録は同梱の `.reg` に委ね、目次と節番号を付けた

### 修正

- `readme.txt` / `README.md` のレジストリ登録の手順で、メモ帳の既定（UTF-8）のまま `.reg` を保存すると、メニュー名の日本語が文字化けするかインポートに失敗する問題。同梱の `.reg` を使えば起きない。自分で作る場合の注意も追記した
- 項目名に書いた `&` が黙って消えていた問題（Win32 のメニューがアクセスキーの目印として解釈するため）。表示したい `&` は `^&` と書けるようになった
- 高 DPI の画面でメニューの文字がぼやける問題。プロセスをモニタごとの DPI に対応させた（Per-Monitor V2）。Windows 10 1703 以降が必要

## [1.0.0] - 2026-08-14

初版公開。

### 機能

- 引数で渡されたファイル / フォルダの種類に応じて、カーソル位置にポップアップメニューを表示し、選ばれたコマンドを起動して終了する（常駐しない、ファイルも書き出さない）
- 1 行 1 項目のテキスト設定ファイル `extrun-config.txt`
  - セクション見出しによる拡張子の指定（`.txt` / `file` / `folder`）と、項目ごとの引き算 `[-.jpg]` / 完全置換 `[.svg]`
  - サブメニュー（`>` `>>`）、セパレーター（`---`）、まとめて実行（`+`）
  - 別名（`@名前 = 値`）、継続行（`|` で始まる行）、作業フォルダ（`:dir`）
  - プレースホルダー `$p` `$-p` `$d` `$n` `$a` `$f` `$e` と、`^` によるエスケープ
- `--check` による設定ファイルの検証（行番号付きの診断、エラーがあれば終了コード 1）
- `--version` / `--help`
- 起動に失敗したときのエラーダイアログ
- exe への VERSIONINFO の埋め込み

### ドキュメント

- `README.md` — 導入と概要
- `extrun-config-format.md` — 設定ファイル形式の完全仕様
- `extrun-recipes.md` — ffmpeg / ImageMagick / IrfanView / 7-Zip / VS Code / VLC / Pandoc などの設定例集と、AutoHotkey から呼び出す例
- `packaging/readme.txt` — 配布 zip に同梱する説明書

[1.6.0]: https://github.com/pirukabocha/extrun/releases/tag/v1.6.0
[1.5.0]: https://github.com/pirukabocha/extrun/releases/tag/v1.5.0
[1.4.0]: https://github.com/pirukabocha/extrun/releases/tag/v1.4.0
[1.3.0]: https://github.com/pirukabocha/extrun/releases/tag/v1.3.0
[1.2.0]: https://github.com/pirukabocha/extrun/releases/tag/v1.2.0
[1.1.0]: https://github.com/pirukabocha/extrun/releases/tag/v1.1.0
[1.0.0]: https://github.com/pirukabocha/extrun/releases/tag/v1.0.0
